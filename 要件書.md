# 要件書 — コースマップ

## 1. 概要
自宅や駅などの起点から出発し、観光スポットや食事処に立ち寄りながら起点に戻ってくるドライブ・散歩コースを、地図上にポイントとルートとして記録・可視化する個人用PWA。

## 2. タイトル
「コースマップ」

## 3. 画面構成
単一画面(全画面地図)+ 左からスライドインするサイドパネル。

| 要素 | 内容 |
|---|---|
| 地図 | Leaflet + OpenStreetMap。クリックでポイント追加 |
| サイドパネル(ルート) | ルートの選択・新規作成・名前変更・削除、移動手段(徒歩/車)の切替 |
| サイドパネル(ポイント一覧) | 現在のルートのポイントを種類アイコン付きで一覧表示。クリックで詳細モーダルを開く |
| サイドパネル(現在地) | 現在地のリアルタイム表示のオン/オフ切替 |
| ポイント詳細モーダル | 種類・名前・メモ(概要)・到着時刻の入力/編集/削除 |

## 4. 主要機能

### 4.1 ポイント管理
- 地図をクリックするとポイント追加モーダルが開き、種類(起点/経由地/観光スポット/食事)・名前・メモ・到着時刻を入力して保存する。
- 起点は1ルートにつき1つのみ登録できる。
- 種類ごとに異なる色・アイコンのマーカーを地図上に表示する。
- ポイントのマーカーまたはサイドパネルの一覧項目をクリックすると詳細モーダルが開き、内容の編集・到着時刻の入力・削除ができる。

### 4.2 ルート管理
- 複数のルートを名前付きで作成・保存・切替・削除できる(最後の1件は削除不可)。
- ルートごとに移動手段(徒歩/車)を設定でき、経路探索の種別に反映される。

### 4.3 経路描画
- 起点→各ポイント(登録順)→起点の周回ルートを、Leaflet Routing Machine(OSRM)で道なりに描画する。
- 経路探索に失敗した場合は、ポイントを結ぶ直線に自動でフォールバックする。

### 4.4 現在地表示
- ブラウザのGeolocation API(`watchPosition`)により、現在地をリアルタイムに地図上へ表示できる(オン/オフ切替可能)。

### 4.5 PWA
- `manifest.webmanifest` と Service Worker により、iPhone Safari等から「ホーム画面に追加」してアプリのように起動できる。
- アプリシェル(HTML/CSS/JS/アイコン)はオフラインでも起動できるようキャッシュするが、地図タイル自体はオンラインが必要。

## 5. データの永続化
- IndexedDB(`course-map-db`)にルート・ポイントを保存。端末間の自動同期は行わない。

## 6. 非機能要件
- ビルドツール・フレームワーク不要。ES Modulesで分割した素のHTML/CSS/JS。
- GPS位置情報取得・PWAインストールの制約上、GitHub Pages等のHTTPS環境での公開が前提(`file://`直接開きは非対応)。
- モバイル端末(iPhone想定)で見やすいレイアウト。

## 7. 将来の機能追加に向けて
- GPSによる実走行/実歩行の軌跡記録(`recordedTrack`)
- 地図タイルのオフラインキャッシュ
- ルートの並べ替えUI(ドラッグ&ドロップ)
