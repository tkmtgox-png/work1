# 要件書 — コースマップ

## 1. 概要
自宅や駅などの起点から出発し、観光スポットや食事処に立ち寄りながら起点に戻ってくるドライブ・散歩コースを、地図上にポイントとルートとして記録・可視化する個人用PWA。

## 2. タイトル
「コースマップ」

## 3. 画面構成
単一画面(全画面地図)+ 左からスライドインするサイドパネル。

| 要素 | 内容 |
|---|---|
| 地図 | Leaflet + OpenStreetMap。クリックでポイント追加。左上に場所検索ボックス(地名入力で地図移動)。右下に現在地へ移動するボタン |
| サイドパネル(ルート) | ルートの選択・新規作成・名前変更・削除、移動手段(徒歩/車)の切替。ルートを切り替えると地図がそのルートの起点へ移動 |
| サイドパネル(ポイント一覧) | 現在のルートのポイントを種類アイコン付きで一覧表示。到着時刻・前のポイントからの移動時間(ルートに含まれる場合のみ)も表示。「順番を最適化」ボタンあり。クリックで詳細モーダルを開く |
| サイドパネル(現在地) | 現在地のリアルタイム表示のオン/オフ切替 |
| ポイント詳細モーダル | 種類・名前・メモ(概要)・到着時刻・滞在時間・人気度(★1〜5)・このルートに含めるか・ルート検索でも固定するかの入力/編集/削除 |
| ルート検索モーダル | 人気度順の観光スポット候補リスト(起点/直近の選択地点からの移動時間つき)。ジャンルで絞り込むプルダウンと、右上に閉じるボタンあり。1件選ぶとルートに反映し、次の候補を再表示 |

## 4. 主要機能

### 4.1 ポイント管理
- 地図をクリックするとポイント追加モーダルが開き、種類(起点/経由地/観光スポット/食事)・名前・メモ・到着時刻・滞在時間(分)・人気度(★1〜5、デフォルト★3)を入力して保存する。滞在時間は種類ごとのデフォルト値(起点/経由地:0分、観光スポット:30分、食事:60分)が自動入力される。
- 起点は1ルートにつき1つのみ登録できる。種類で「起点」を選ぶと名前欄に自動で「起点」と入力される。
- 地図クリックでのポイント追加時は、その地点の地名をNominatim逆ジオコーディングで自動入力する(手入力で上書き可能)。
- 「ルート検索でも固定(必ず含める)」をオンにすると、そのポイントはルート検索を実行してもリセット・除外されず、常にルートに残る(「絶対行きたい場所」を確保しておく用途)。
- 種類ごとに異なる色・アイコンのマーカーを地図上に表示する。「このルートに含める」がオフのポイントは半透明表示になる。
- ポイントのマーカーまたはサイドパネルの一覧項目をクリックすると詳細モーダルが開き、内容の編集・到着時刻/滞在時間の入力・「このルートに含める」の切替・削除ができる。

### 4.2 ルート管理
- 複数のルートを名前付きで作成・保存・切替・削除できる(最後の1件は削除不可)。
- ルートごとに移動手段(徒歩/車)・希望時間(分)を設定できる。

### 4.3 経路描画
- 起点→各ポイント(登録順、「このルートに含める」がオンのもののみ)→起点の周回ルートを、Leaflet Routing Machine(OSRM)で道なりに描画する。
- 経路探索に失敗した場合は、ポイントを結ぶ直線に自動でフォールバックする。

### 4.4 ルート検索(観光スポットの対話式選択+周辺自動検索)
- 起点・希望時間(分)を設定した状態で「ルート検索」を押すと、「ルート検索でも固定」がオンのポイント(下記4.1参照)を除く全観光スポットが未選択状態にリセットされ、候補選びのモーダルが開く。固定ポイントは時間判定の対象外で必ずルートに残り、複数ある場合は起点からの効率的な巡回順が自動で決まる。
- 候補リストには、①ルートに登録済みの観光スポットと、②現在位置周辺をOpenStreetMapのデータから自動検索した観光スポット(神社・博物館・動物園など)の両方が1つのリストにまとまって表示される。どちらも希望時間内(起点への戻り時間を含む)に収まるものだけを、現在位置(最初は起点、選択が進むと直近に選んだ観光スポット)からの**移動時間が近い順**で表示する。滞在時間はこの時間判定には使わない。
- 登録済み候補には人気度(★1〜5)、周辺自動検索の候補にはジャンル(神社・博物館・動物園等)を表示する。
- 候補を1つ選ぶと、そのスポットがルートに反映される(周辺自動検索由来の場合は新しい観光スポットとして自動登録され、人気度は★3の仮値になる。後から編集可能)。選んだスポットを新たな現在位置として候補リストが再計算される。
- 候補が無くなると自動的に終了メッセージが表示される。「ここで確定」ボタン、または右上の「×」ボタンでいつでも検索を終了できる(どちらも同じ動作。選択内容はその都度保存済み)。
- モーダル上部のプルダウンで、候補に含まれるジャンル(神社・博物館等)のいずれかに絞り込んで表示できる(「すべて」で解除)。絞り込みは表示上のフィルタのみで、再検索は行わない。
- 選択されなかった登録済み観光スポットは削除されず、「このルートに含める」がオフになった状態でルートから除外される(後から手動で戻すことも可能)。
- 周辺自動検索が失敗・応答なしの場合は、登録済み候補のみで続行する。

### 4.5 巡回順の最適化
- サイドパネルの「順番を最適化」ボタンを押すと、現在ルートに含まれている観光スポット(固定ポイント・ルート検索で選んだポイントを問わず)を、起点から効率よく回れる順番に自動で並べ替える。実際の道路事情は考慮しない直線距離ベースの概算。
- 経由地・食事の順序・位置は変更しない。
- 観光スポットが2件未満の場合は「最適化できる観光スポットが2件以上ありません」と案内する。
- ルート検索を使わず手動でポイントを追加しただけの場合でも利用できる。

### 4.6 場所の検索
- 地図左上の検索ボックスに地名・住所を入力すると、Nominatim順ジオコーディングにより候補地点を最大5件リスト表示する。
- 候補を選ぶと地図がその地点へ移動し、目印として一時的なマーカーを表示し、検索ボックスの入力文字は自動でクリアされる。
- 該当なしの場合は「見つかりませんでした」と表示する。
- サイドパネルを開いている間は検索ボックスを一時的に隠す(パネルと重なって隠れてしまうのを避けるため)。パネルを閉じると再表示される。
- 目印マーカーをタップするとポップアップにボタンが表示され、その地点・名称があらかじめ入力された状態でポイント追加モーダルが開く(地図クリックによる従来のポイント追加も引き続き利用できる)。
  - 現在のルートに起点が未登録の場合: 「ポイントとして追加」ボタン(種類は「起点」が初期選択)
  - 起点登録済みの場合: 「観光スポットとして追加」(現在のルートに観光スポットとして追加)と「新しいルートの起点として追加」(自動命名で新しいルートを作成・切替してから、その起点として追加)の2つ

### 4.7 現在地表示
- ブラウザのGeolocation API(`watchPosition`)により、現在地をリアルタイムに地図上へ表示できる(オン/オフ切替可能、初期状態はON)。
- 地図上のボタンをタップすると、現在地を一度取得して地図をそこへ移動できる(リアルタイム表示のオン/オフに関わらず利用可能)。

### 4.8 PWA
- `manifest.webmanifest` と Service Worker により、iPhone Safari等から「ホーム画面に追加」してアプリのように起動できる。
- アプリシェル(HTML/CSS/JS/アイコン)はオフラインでも起動できるようキャッシュするが、地図タイル自体はオンラインが必要。

## 5. データの永続化
- IndexedDB(`course-map-db`)にルート・ポイントを保存。端末間の自動同期は行わない。

## 6. 非機能要件
- ビルドツール・フレームワーク不要。ES Modulesで分割した素のHTML/CSS/JS。
- GPS位置情報取得・PWAインストールの制約上、GitHub Pages等のHTTPS環境での公開が前提(`file://`直接開きは非対応)。
- モバイル端末(iPhone想定)で見やすいレイアウト。

## 7. 将来の機能追加に向けて
- GPSによる実走行/実歩行の軌跡記録(`recordedTrack`)
- 地図タイルのオフラインキャッシュ
- ルートの並べ替えUI(ドラッグ&ドロップ)
