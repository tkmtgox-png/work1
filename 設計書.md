# 設計書 — コースマップ

## 1. ファイル構成
```
index.html          画面全体のマークアップ
style.css           スタイル
manifest.webmanifest PWAマニフェスト
sw.js                Service Worker(アプリシェルのキャッシュ)
icons/               PWAアイコン(icon-192.png / icon-512.png)
js/
  app.js             エントリーポイント。状態管理とイベント配線
  map.js             Leaflet地図の初期化、現在地トラッカー
  points.js          ポイントの種類定義・マーカーアイコン・一覧描画
  history.js         実行履歴(いつ回ったか)のデータ生成・一覧描画
  routing.js         Leaflet Routing Machineでの経路描画、直線フォールバック
  storage.js         IndexedDB CRUD
  geocode.js         Nominatim逆ジオコーディング・順ジオコーディング(場所検索)
  route-search.js    ルート検索(登録済み候補の抽出、検索半径の推定、巡回順の最適化)
  nearby-search.js   Overpass APIでの周辺観光スポット検索・ジャンル判定
```

## 2. データモデル(IndexedDB: `course-map-db`, store: `routes`)
```js
{
  id: string,               // crypto.randomUUID()
  name: string,
  transportMode: "walk" | "car",
  desiredMinutes: number | null,  // ルート検索機能で使う希望時間(分)
  createdAt: number,
  updatedAt: number,
  points: [
    {
      id: string,
      type: "start" | "waypoint" | "sightseeing" | "meal",
      name: string,
      memo: string,
      lat: number,
      lng: number,
      order: number,        // 起点以外の訪問順
      arrivalTime: string,  // "HH:MM" 形式、未入力可
      stayMinutes: number,  // 滞在時間(分)。種類ごとのデフォルト値あり
      included: boolean,    // このルートの周遊に含めるか(既定true)
      popularity: number,   // 人気度 1〜5(既定3)。ルート検索の並び替えに使用
      genre: string,        // ジャンル(神社・博物館等)。周辺自動検索で登録された場合のみ設定
      locked: boolean,      // ルート検索でも固定(必ず含める)か(既定false)
    }
  ],
  history: [
    {
      id: string,          // crypto.randomUUID()
      timestamp: number,   // Date.now()のミリ秒。実際に回った日時
      memo: string,        // 任意の一言メモ
    }
  ]
}
```
`history`はIndexedDBのスキーマ・バージョンを変えずに`points`と同様ルートオブジェクトの1フィールドとして追加したもの。既存(`history`未設定)のルートは参照側で`route.history || []`として扱う。

## 3. 主要な処理フロー

### 3.1 初期化(`app.js: init()`)
1. `initMap()` で地図を表示(現在地取得できればそこを中心に)
2. `getAllRoutes()` でIndexedDBから全ルートを取得。0件なら初期ルートを1件作成
3. ルート選択セレクトボックス・ポイント一覧・マーカー・経路線を描画
4. Service Workerを登録
5. 「現在地をリアルタイム表示」トグル(`#track-location-toggle`)は`checked`をデフォルトにしており、そのチェック状態に合わせて`locationTracker.start()`を自動実行する(手動でトグルをONにした時と同じ処理)

### 3.1.1 ルート切り替え時に起点へ移動(`app.js: focusOnRouteStart`)
`els.routeSelect`のchange・新規ルート作成・ルート削除の3箇所(いずれも`currentRoute`が別のルートに切り替わるタイミング)で`focusOnRouteStart(currentRoute)`を呼ぶ。中身は`route.points`から`type === "start"`を探し、あれば`map.setView([lat,lng], 15)`するだけ(起点が無ければ何もしない)。初期化時(`init()`内で最初のルートを選ぶ処理)には適用していない。`initMap()`内の非同期GPS初期センタリングと競合し、どちらが最後に地図を動かすか不定になってちらつく恐れがあるため。

### 3.1.2 現在地に移動ボタン(`map.js: locateOnce`)
地図右下にフローティング配置した`#locate-btn`をクリックすると`locateOnce(map, onStatus)`を呼ぶ。中身は一度きりの`navigator.geolocation.getCurrentPosition`で、成功時は`map.setView([lat,lng], DEFAULT_ZOOM)`、失敗時は`createLocationTracker`と共有しているプライベート関数`describeGeolocationError`でエラーメッセージを組み立てて`onStatus`に渡す(表示先は既存の`#location-status`)。リアルタイム追跡のON/OFFとは独立して常に使える。

### 3.2 ポイント追加
1. 地図クリック(`map.on("click")`)で `openPointModal({mode: "add", latlng})`
2. フォーム送信時、`newPoint()` でポイントオブジェクトを生成し `currentRoute.points` に追加
3. `saveRoute()` でIndexedDBへ保存 → 再描画

### 3.2.1 ポイント一覧の移動時間表示(`points.js: renderPointList`)
一覧の各行には、滞在時間ではなく「前のポイントからの移動時間」を表示する。`routing.js: buildOrderedPoints(route)`(経路描画で使う実際のルート順、起点を先頭に`included !== false`のポイントを`order`順で並べたもの)をそのまま使い、隣り合う2点ごとに`route-search.js: travelMinutes`で移動時間を計算して`Map<pointId, minutes>`を作る。起点(先頭)と、除外中(`included === false`、`buildOrderedPoints`の結果に含まれない)ポイントには値が無いため移動時間欄は空になる。

### 3.3 ポイント編集・削除
- 地図上のマーカーのクリックで `openPointModal({mode: "edit", point})`
- 保存: 該当ポイントのフィールドを更新して `saveRoute()`
- 削除: `currentRoute.points` から除外して `saveRoute()`

### 3.3.1 ポイント一覧クリックで地図フォーカス(`renderAll`内の`renderPointList`の`onSelect`)
一覧項目をクリックしても編集モーダルは開かない(モーダルは画面全体を覆い地図を同時に見られないため)。代わりに`map.setView([point.lat, point.lng], 17)`で地図をそのポイントへ移動し、`markers`(ポイントID→Leafletマーカーの`Map`)から該当マーカーを取得して`.bindPopup(escapeHtml(point.name || "")).openPopup()`でポップアップを開く。ポイント数が多い時にどれがどこにあるか確認しやすくするための機能。編集は引き続き地図上のマーカークリックから行う

### 3.3.2 実行履歴(`history.js` + `app.js`)
「いつこのコースを実際に回ったか」を手動で記録する機能。GPSでの自動検知は判定ロジックが複雑・誤検知が出やすいため採用していない。
1. サイドパネルの「実行履歴」セクション(`#history-section`、ルートセクションの直下)の「記録する」ボタン(`#record-visit-btn`)クリックで`openHistoryModal({mode:"add"})`。現在時刻(`Date.now()`を`toDatetimeLocalValue()`で`datetime-local`用文字列に変換)を初期値にした`#history-modal`を開く
2. フォーム送信(`handleHistoryFormSubmit`): `#history-datetime`の値を`new Date(value).getTime()`でミリ秒に変換し、新規なら`newHistoryEntry({timestamp, memo})`(`history.js`)を`currentRoute.history`に追加、編集なら既存エントリのフィールドを更新。`saveRoute()`後に`renderAll()`
3. 一覧(`#history-list`、`history.js: renderHistoryList`)は`timestamp`の降順で表示。項目クリックで`openHistoryModal({mode:"edit", entry})`を開き、`#history-delete-btn`(新規時は非表示)から`handleHistoryDelete`で削除できる
4. `history`は`points`と同じくルートオブジェクトの1フィールドのため、`renderAll()`はルート切り替え時にも呼ばれ、常に`currentRoute`(切替後のルート)の履歴だけが表示される

### 3.4 経路描画(`routing.js`)
1. `buildOrderedPoints(route)`: 起点を先頭・末尾に、その他を`order`順に並べた配列を生成(起点が無ければ単純な順路)。`included === false` の観光スポットはここで除外される
2. `L.Routing.control` でOSRM経由の道なりルートを取得する(`profile`はルートの`transportMode`(`walk`→`foot`, `car`→`driving`)に対応)。コントロール自体の既定の線描画は透明にし、`routesfound`イベントで得た`route.coordinates`/`route.waypointIndices`を使って、往路(起点→最後の実ポイント、青実線)と復路(最後の実ポイント→起点、オレンジ破線)を別々の`L.polyline`として自前で描画する(`drawSegments`)。往復が同じ道を通る場合でも視覚的に区別できるようにするため
3. `routingerror` イベント発生時は往路・復路の線を除去し、`L.polyline` による直線フォールバックに切り替え
4. `clearRoute(map)`(ポイントが2点未満になった時に呼ばれる): コントロール自体は`map.removeControl()`で破棄せず、`routingControl.setWaypoints([])`で経路だけ空にする。直前の`setWaypoints()`で投げたOSRMへのリクエストがまだ飛んでいる状態でコントロールを物理的に破棄すると、応答が返ってきた時点でLeaflet Routing Machine内部が既に破棄済みの参照にアクセスして`Cannot read properties of null (reading 'removeLayer')`例外を出すことがあったため(ポイント追加直後に削除する、といった素早い操作で再現)。`getControl`の「プロフィールが変わらない限りコントロールを使い回す」方針と同じ理由による対応

### 3.5 現在地表示(`map.js: createLocationTracker`)
- トグルONで `navigator.geolocation.watchPosition` を開始し、専用マーカー(青丸)の位置を継続更新
- トグルOFFで `clearWatch` しマーカーを除去

### 3.6 ルート検索(`route-search.js` + `nearby-search.js` + `app.js`の`openRouteSearch`/`renderCandidateList`/`selectCandidate`)
対話式に観光スポットを1つずつ選んでいく方式。状態は`app.js`のモジュール変数`routeSearch = { currentPos, usedMinutes, nextOrder, lastMerged }`で保持する。候補リストの取得は非同期(Overpass APIへのfetchを含む)なので、`candidateToken`で古い応答を無視するガードを入れている(`fillNameFromMap`の`geocodeToken`と同じパターン)。

1. `openRouteSearch()`: 起点・`desiredMinutes`をチェックし、`locked`な観光スポットは`included = true`・それ以外は`included = false`にリセット。経由地・食事の最大`order`+1を`nextOrder`の基準にしたうえで、固定ポイント一覧を`optimizeVisitOrder(start, lockedPoints)`(3.6.1参照)で並べ替え、起点から順に`travelMinutes`を積算しながら`order`を採番・`nextOrder`を進める。その結果の到達地点・使用時間を`routeSearch`の初期値(`currentPos`/`usedMinutes`/`nextOrder`/`lastMerged=[]`)にしてから保存し、ジャンル絞り込み(`#route-search-genre-filter`)を「すべて」にリセットして候補モーダルを表示(固定ポイントが無ければ`currentPos`=起点・`usedMinutes`=0で従来と同じ)
2. `renderCandidateList()`(非同期。Overpass呼び出しを含む「取得」部分のみ担当):
   - 登録済み候補: `getFeasibleCandidates(route, routeSearch.currentPos, routeSearch.usedMinutes)`で、希望時間内(戻り時間込み)に収まる未選択の観光スポットを取得(`genre`も含めてマッピング。この時点ではソートしない)
   - 周辺検索候補: `estimateSearchRadiusKm(remaining, transportMode)`で半径を決め、`searchNearby(routeSearch.currentPos, radius)`をawait。半径は残り時間をそのまま片道距離とみなして概算する(往復前提で半分にはしない。起点方向の候補は「帰り道への合流」で済むぶん現在地からは遠くても間に合うことがあるため、検索網は広めに取り、実際の足切りは次のtotalIfChosen判定に任せる。8kmの上限は維持)。結果から、登録済みポイントに30m以内で近いものを除外し、希望時間内に収まるものだけを残す
   - 2つを合わせて`travelFromCurrent`(現在位置からの移動時間)の昇順にソートし、`routeSearch.lastMerged`に保存。`updateGenreFilterOptions(merged)`でジャンルの選択肢を再構築し、`renderFilteredCandidates()`で実際の描画を行う
3. `updateGenreFilterOptions(merged)`(同期): `merged`から`genre`の重複無し一覧を作って`#route-search-genre-filter`の`<option>`を再構築する。直前に選んでいたジャンルが新しい一覧にも存在すれば維持し、無ければ「すべて」に戻す
4. `renderFilteredCandidates()`(同期。`#route-search-genre-filter`の`change`イベントからも呼ばれる、Overpassへの再検索はしない): `routeSearch.lastMerged`を現在選択中のジャンルでクライアント側フィルタし、登録済みは★人気度、周辺検索由来はジャンルを表示してリスト描画。候補(`lastMerged`)自体が0件なら終了メッセージ、絞り込みの結果0件ならその旨のメッセージを出し分ける
5. `selectCandidate(candidate)`: `candidate.source`が`"registered"`なら既存ポイントを`included = true`・`order = routeSearch.nextOrder++`に更新、`"osm"`なら`newPoint()`で新規観光スポット(`popularity: 3`、`genre`をセット)を作成し`currentRoute.points`に追加。どちらも保存後、`routeSearch.currentPos`をそのポイントへ、`usedMinutes`に移動時間を加算 → `renderAll()`→ `renderCandidateList()`で候補を再計算(ここで滞在時間は一切使わない)
6. `closeRouteSearch()`(「ここで確定」`#route-search-finish`、右上の「×」`#route-search-close`のどちらからも同じ関数が呼ばれ、動作の違いは無い): `candidateToken`を進めて古い非同期処理を無効化し、モーダルを閉じて`routeSearch`をクリア。選択内容はステップ5の時点で保存済みのため追加の保存処理は不要
7. 除外されたポイントは`renderMarkers`/`renderPointList`側で半透明表示(`.marker-excluded` / `#point-list li.excluded`)になる。経由地・食事はこのフローの対象外で、`order`・`included`とも変更されない

### 3.6.1 巡回順の最適化(`route-search.js: optimizeVisitOrder` + `app.js: handleOptimizeOrder`)
- `optimizeVisitOrder(startPoint, points)`: 最近傍法(`haversineKm`が最小の未訪問点を順に選ぶ)で初期巡回順を作り、2-opt改善(`tour`内の2点`i<j`の区間を反転させて総距離(起点→...→起点の閉路)が縮むなら採用、を改善が無くなるまで全区間総当たりで繰り返す)を適用して返す。`transportMode`は受け取らない(速度差は全区間一律のため巡回順の優劣に影響しない)
- 呼び出し元1: `openRouteSearch()`(3.6のステップ1)。固定ポイントの巡回順決めに使う
- 呼び出し元2: `handleOptimizeOrder()`(「順番を最適化」ボタン`#optimize-order-btn`、非同期): 起点必須チェック→`type==="sightseeing" && included !== false`のポイントを収集(2件未満なら「最適化できる観光スポットが2件以上ありません」とalertして終了)→経由地・食事の最大`order`+1を基準に`optimizeVisitOrder(start, points)`の結果順で`order`を採番し直す→`saveRoute()`→`renderAll()`。経由地・食事自体の`order`は変更しない(新規選択は必ずそれらの後、という既存の並び規則を踏襲)

### 3.7 場所の検索(`geocode.js: searchPlaces` + `app.js`の`handlePlaceSearch`/`selectPlaceResult`)
1. 検索ボックス(`#place-search`)のフォーム送信で`handlePlaceSearch()`が呼ばれ、`searchPlaces(query)`(Nominatim `/search`)を非同期で呼ぶ。他の非同期処理と同じ`placeSearchToken`インクリメント方式で古い応答を無視する
2. 結果(最大5件)を`#place-search-results`にリスト表示。0件なら「見つかりませんでした」
3. 候補クリックで`selectPlaceResult(result)`: `map.setView([lat,lng], 17)`で地図移動し、既存の目印マーカー(`searchResultMarker`)があれば除去してから、`buildSearchResultPopup(result)`で組み立てたポップアップ付きの新しいマーカーを1つ立てて`#place-search-input`を空にする
4. `buildSearchResultPopup(result)`: `hasStart = currentRoute.points.some(p => p.type === "start")`で分岐し、ボタンを組み立てる
   - `hasStart`が`false`: 「ポイントとして追加」ボタン1つ。クリックで`openPointModal({mode:"add", latlng:{lat,lng}, defaultType:"start", defaultName: result.name})`
   - `hasStart`が`true`: 「観光スポットとして追加」(`defaultType:"sightseeing"`、現在の`currentRoute`にそのまま追加)と「新しいルートの起点として追加」(`createAndSwitchToNewRoute(`ルート${routes.length+1}`)`で新しいルートを作成・`currentRoute`を切替してから`defaultType:"start"`で`openPointModal`)の2つ
   - `openPointModal`は`defaultType`/`defaultName`が渡されるとその値で種類・名前欄を初期化し、`defaultName`がある場合は`fillNameFromMap`(逆ジオコーディング)を呼ばない。以降の保存処理(起点重複チェック含む)は通常のポイント追加と共通
5. `createAndSwitchToNewRoute(name)`(新規): `createEmptyRoute(name)`→`saveRoute`→`routes.push`→`currentRoute`を切替→`refreshRouteSelect()`→`renderAll()`→`focusOnRouteStart(currentRoute)`をまとめた共通関数。サイドパネルの「＋新規ルート」ボタン(`prompt()`で名前を聞いてから呼ぶ)と、上記「新しいルートの起点として追加」ボタン(`ルート${routes.length+1}`で自動命名して呼ぶ)の両方から使う
6. `handlePointFormSubmit`の保存成功時に`clearSearchResultMarker()`を呼び、検索由来でポイントを追加した後に一時マーカーと新しいポイントのマーカーが同じ座標で重複表示されるのを防ぐ(通常のポイント追加時は`searchResultMarker`が`null`のため無害)
7. サイドパネル(`#panel`)を開閉するタイミングで、検索ボックス(`#place-search`)・結果ドロップダウン(`#place-search-results`)に汎用の`.hidden`クラスを付け外しする(`panelToggle`で追加、`panelClose`で`#place-search`のみ除去)。`#panel`のz-index(1100)が`.place-search`(1000)より高く、パネル幅が画面幅の85%まで広がり得るため、パネルを開くと検索ボックスが重なって隠れてしまう問題への対応

## 4. 既知の制約・注意事項
- OSRM公開デモサーバーは評価用途のため、将来的にリクエスト過多で不安定になる可能性がある。その場合は `routing.js` の `serviceUrl` を OpenRouteService 等の代替(要APIキー登録)に差し替える。
- `file://` 直接開きでは Geolocation・Service Worker が動作しないため、必ずHTTPS環境(GitHub Pages)で利用する。
- 地図タイル自体はキャッシュしていないため、オフライン時は地図画像が表示されない。
- ルート検索の所要時間見積もり・「順番を最適化」の巡回順(`optimizeVisitOrder`)はいずれも直線距離ベースの簡易計算であり、実際の道路事情(信号・坂道・渋滞・一方通行等)は考慮していない。あくまで目安。滞在時間も計算に含めていない。
- 周辺自動検索(Overpass API公開インスタンス)は無料だが応答が遅い・不安定な場合がある。失敗時は登録済み候補のみで続行する(`nearby-search.js`)。
- OSMに`name`タグが無い施設は周辺検索の候補に出てこない。ジャンル判定もOSMのタグ付け次第で実際とズレることがある。
- 場所の検索(Nominatim `/search`)も無料の公開インスタンスを利用しているため、短時間に連続で検索すると応答が遅くなる・失敗する場合がある。失敗時は空の結果として扱う(`geocode.js: searchPlaces`)。
